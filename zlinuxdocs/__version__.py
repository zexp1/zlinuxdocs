# The single source of truth for the version is the repo-root VERSION file.
# build-deb.sh regenerates this file from it at build time. Keep them in sync;
# build-deb.sh fails loudly if they disagree.
__version__ = "0.1.0"
